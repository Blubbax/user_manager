apiVersion: apps/v1
kind: Deployment
metadata:
  name: mm-user-api
spec:
  template:
    spec:
      containers:
        - name: mm-user-api
          resources:
            limits:
              cpu: 400m
            requests:
              cpu: 200m
